// Cron removed: database backup is no longer scheduled by application runtime.
// This module is kept as a no-op placeholder to avoid broken imports in legacy environments.

const JOB_KEY = './src/cron/cronDbBackup.js';

export { JOB_KEY };
export default null;
